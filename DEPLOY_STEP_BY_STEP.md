# 🚀 Пошаговая инструкция деплоя на Railway

## 📋 Подготовка (5 минут)

### Шаг 1: Проверьте, что код в Git

```bash
cd "/Users/dmitry/Desktop/Memoraid 2"
git status
```

Если есть несохраненные изменения:
```bash
git add .
git commit -m "Prepare for production deployment"
git push origin main
```

### Шаг 2: Сгенерируйте ключ шифрования

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**Сохраните этот ключ!** Он понадобится позже.

---

## 🚂 Деплой на Railway (15 минут)

### Шаг 3: Регистрация на Railway

1. Откройте [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Авторизуйтесь через GitHub
5. Выберите ваш репозиторий `Memoraid 2`

Railway автоматически определит проект и начнет деплой.

---

### Шаг 4: Настройка Backend сервиса

1. В проекте Railway найдите сервис (обычно называется по имени репозитория)
2. Нажмите на сервис → **"Settings"**
3. Настройте:

**Root Directory:** `backend`

**Build Command:**
```bash
cd backend && pnpm install && pnpm prisma generate && pnpm build
```

**Start Command:**
```bash
node dist/main.js
```

---

### Шаг 5: Добавление PostgreSQL

1. В проекте Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст БД и переменную `DATABASE_URL`

**Важно:** После создания БД нужно установить расширение `pgvector`:

1. Откройте PostgreSQL сервис
2. Перейдите в **"Data"** → **"Query"**
3. Выполните SQL:

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

Или через Railway CLI:

```bash
# Установка Railway CLI
npm i -g @railway/cli

# Логин
railway login

# Подключение к проекту
railway link

# Подключение к PostgreSQL
railway connect postgres

# В открывшемся psql выполните:
CREATE EXTENSION IF NOT EXISTS vector;
\q
```

---

### Шаг 6: Добавление Redis

1. В проекте Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"Add Redis"**
3. Railway автоматически создаст Redis и переменную `REDIS_URL`

---

### Шаг 7: Настройка переменных окружения

Откройте Backend сервис → **"Variables"** → добавьте:

```env
# Database (Railway автоматически создает)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (Railway автоматически создает)
REDIS_URL=${{Redis.REDIS_URL}}

# Telegram
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather

# Gemini API
GEMINI_API_KEY=ваш_ключ_Gemini

# Шифрование (сгенерированный ключ из шага 2)
ENCRYPTION_KEY=ваш_64_символьный_ключ

# Environment
NODE_ENV=production
PORT=3001

# Frontend URL (будет создан после деплоя frontend)
NEXT_PUBLIC_BACKEND_URL=https://your-backend.railway.app
```

**Как добавить переменные:**
1. Нажмите **"+ New Variable"**
2. Введите имя (например, `TELEGRAM_BOT_TOKEN`)
3. Введите значение
4. Повторите для всех переменных

---

### Шаг 8: Деплой Frontend

1. В проекте Railway нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите тот же репозиторий `Memoraid 2`
3. В настройках сервиса:

**Root Directory:** `frontend`

**Build Command:**
```bash
cd frontend && pnpm install && pnpm build
```

**Start Command:**
```bash
pnpm start
```

**Variables:**
```env
NODE_ENV=production
NEXT_PUBLIC_BACKEND_URL=https://your-backend.railway.app
PORT=3000
```

---

### Шаг 9: Инициализация базы данных

После первого деплоя backend нужно применить миграции:

**Вариант A: Через Railway CLI**

```bash
# Если еще не установлен
npm i -g @railway/cli

# Логин
railway login

# Подключение к проекту
railway link

# Применение миграций
railway run --service backend "cd backend && pnpm prisma migrate deploy"
```

**Вариант B: Через Railway Dashboard**

1. Откройте Backend сервис
2. Перейдите в **"Deployments"**
3. Нажмите на последний деплой
4. Откройте **"View Logs"**
5. Найдите ошибки или подтвердите успешный деплой

Если миграции не применены автоматически, добавьте в **Start Command**:

```bash
cd backend && pnpm prisma migrate deploy && node dist/main.js
```

---

### Шаг 10: Получение доменов

Railway автоматически создает домены:
- Backend: `your-backend.railway.app`
- Frontend: `your-frontend.railway.app`

**Найдите домены:**
1. Откройте Backend сервис → **"Settings"** → **"Networking"**
2. Скопируйте домен (например, `memoraid-backend-production.up.railway.app`)
3. Откройте Frontend сервис → **"Settings"** → **"Networking"**
4. Скопируйте домен (например, `memoraid-frontend-production.up.railway.app`)

---

### Шаг 11: Обновление переменных окружения

Обновите переменные с реальными доменами:

**Backend:**
```env
NEXT_PUBLIC_BACKEND_URL=https://your-frontend.railway.app/api
```

**Frontend:**
```env
NEXT_PUBLIC_BACKEND_URL=https://your-backend.railway.app
```

---

### Шаг 12: Настройка Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/myapps`
3. Выберите вашего бота
4. Отправьте `/editapp`
5. Выберите вашего бота
6. Выберите **"Web App URL"**
7. Введите домен frontend: `https://your-frontend.railway.app`
8. Сохраните изменения

---

### Шаг 13: Проверка работы

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку "Open App" или отправьте `/start`
3. Проверьте работу:
   - ✅ Создание заметок
   - ✅ Календарь
   - ✅ Советы
   - ✅ Поиск

---

## 🔍 Проверка логов

Если что-то не работает:

1. Откройте сервис в Railway
2. Перейдите в **"Deployments"**
3. Выберите последний деплой
4. Нажмите **"View Logs"**

Или через CLI:
```bash
railway logs --service backend
railway logs --service frontend
```

---

## 🆘 Решение проблем

### Проблема: Build падает

**Решение:**
1. Проверьте логи в Railway
2. Убедитесь, что `Root Directory` правильный (`backend` или `frontend`)
3. Проверьте, что все зависимости в `package.json`

### Проблема: База данных не подключается

**Решение:**
1. Проверьте `DATABASE_URL` в переменных окружения
2. Убедитесь, что расширение `pgvector` установлено (см. Шаг 5)
3. Проверьте, что миграции применены (см. Шаг 9)

### Проблема: 401 ошибка в Telegram

**Решение:**
1. Убедитесь, что `NODE_ENV=production` (не `development`)
2. Проверьте `TELEGRAM_BOT_TOKEN`
3. Проверьте, что Mini App URL правильный в BotFather

### Проблема: Frontend не подключается к Backend

**Решение:**
1. Проверьте `NEXT_PUBLIC_BACKEND_URL` в Frontend переменных
2. Убедитесь, что домен backend правильный
3. Проверьте CORS настройки (должны быть включены)

---

## 📝 Чеклист перед запуском

- [ ] Код закоммичен и запушен в Git
- [ ] Ключ шифрования сгенерирован
- [ ] Railway проект создан
- [ ] Backend сервис настроен
- [ ] PostgreSQL добавлен и `pgvector` установлен
- [ ] Redis добавлен
- [ ] Все переменные окружения заполнены
- [ ] Frontend сервис настроен
- [ ] Миграции применены
- [ ] Домены получены
- [ ] Telegram Mini App URL настроен в BotFather
- [ ] Приложение протестировано

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет работать в Telegram!

**Следующие шаги:**
- Мониторинг логов
- Тестирование всех функций
- Настройка кастомного домена (опционально)
- Настройка резервного копирования БД

