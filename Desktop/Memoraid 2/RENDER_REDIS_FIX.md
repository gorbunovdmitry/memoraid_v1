# 🔴 Как создать Redis в Render (обновлено)

## ⚠️ Важно!

В Render **нет** отдельного сервиса "Redis". Вместо этого используется **"Key Value"** (Valkey), который полностью совместим с Redis.

## ✅ Правильные шаги:

### 1. Создайте Key Value сервис

1. В Render Dashboard нажмите **"+ New"**
2. В меню найдите и выберите **"Key Value"** (не Redis!)
3. Заполните:
   - **Name**: `memoraid-keyvalue`
   - **Region**: Тот же, что и для PostgreSQL
   - **Plan**: `Free` или `Starter`
4. Нажмите **"Create Key Value"**

### 2. Получите REDIS_URL

После создания:
1. Откройте созданный Key Value сервис
2. Перейдите в **"Info"** или **"Connections"**
3. Найдите **"Internal Key Value URL"**
4. Скопируйте значение - это ваш `REDIS_URL`

### 3. Используйте в Backend

Добавьте `REDIS_URL` в Environment Variables вашего Backend сервиса. Код будет работать без изменений, так как Valkey полностью совместим с Redis!

---

## 🔍 Если не видите "Key Value" в меню:

1. Убедитесь, что вы на главной странице Dashboard
2. Попробуйте обновить страницу (F5)
3. Проверьте, что у вас есть доступ к созданию сервисов (не на Free плане с ограничениями)
4. Если все равно не видите - используйте альтернативу (см. ниже)

---

## 🔄 Альтернатива: Внешний Redis

Если Key Value недоступен, можно использовать внешний Redis:

### Upstash Redis (бесплатно):
1. Зарегистрируйтесь на [upstash.com](https://upstash.com)
2. Создайте Redis database
3. Скопируйте `REDIS_URL`
4. Используйте в Render Environment Variables

### Redis Cloud (бесплатно):
1. Зарегистрируйтесь на [redis.com/cloud](https://redis.com/cloud)
2. Создайте бесплатную БД
3. Скопируйте `REDIS_URL`
4. Используйте в Render Environment Variables

---

## ✅ Проверка

После настройки ваш `REDIS_URL` должен выглядеть примерно так:
```
rediss://default:password@host:port
```

Или для внешнего сервиса:
```
redis://user:password@host:port
```

