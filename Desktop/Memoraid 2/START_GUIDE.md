# 📖 Подробная инструкция по запуску проекта Memoraid

## 🎯 Для кого эта инструкция
Эта инструкция написана для тех, кто впервые работает с этим проектом. Мы разберем каждый шаг подробно.

---

## 📋 Что вам понадобится

1. **Терминал** (Terminal) - встроенная программа на Mac
2. **Docker Desktop** - программа для запуска контейнеров
3. **Node.js и pnpm** - уже должны быть установлены

---

## 🔍 Шаг 0: Открыть проект в терминале

### Что такое терминал?
Терминал - это текстовый интерфейс для работы с компьютером через команды.

### Как открыть терминал на Mac:
1. Нажмите `Cmd + Пробел` (откроется поиск Spotlight)
2. Введите "Terminal" или "Терминал"
3. Нажмите Enter

### Как перейти в папку проекта:

В терминале введите команду (скопируйте и вставьте):

```bash
cd ~/Desktop/Memoraid\ 2
```

**Что делает эта команда:**
- `cd` = "change directory" (перейти в папку)
- `~/Desktop/Memoraid\ 2` = путь к вашему проекту
- `\` перед пробелом нужен, чтобы терминал понял, что пробел - часть названия

**Проверка:** После выполнения команды вы должны увидеть что-то вроде:
```
dmitry@MacBook Memoraid 2 %
```

---

## 🐳 Шаг 1: Установка и запуск Docker Desktop

### Что такое Docker?
Docker - это программа, которая запускает базу данных и другие сервисы в "контейнерах" (изолированных средах).

### Проверка, установлен ли Docker:

В терминале выполните:
```bash
docker --version
```

**Если видите версию** (например, `Docker version 24.0.0`):
- ✅ Docker установлен, переходите к следующему шагу

**Если видите ошибку** (например, `command not found`):
- ❌ Нужно установить Docker Desktop

### Установка Docker Desktop:

1. Откройте браузер и перейдите на: https://www.docker.com/products/docker-desktop
2. Скачайте Docker Desktop для Mac
3. Установите программу (просто перетащите в Applications)
4. Запустите Docker Desktop из Applications
5. Дождитесь, пока Docker полностью запустится (иконка в верхней панели должна быть зеленая)

**Важно:** Docker Desktop должен быть запущен перед выполнением следующих шагов!

---

## 🚀 Шаг 2: Запуск Docker контейнеров

### Что мы делаем?
Запускаем базу данных PostgreSQL и Redis в Docker контейнерах.

### Выполните команды:

**Убедитесь, что вы в папке проекта:**
```bash
cd ~/Desktop/Memoraid\ 2
```

**Перейдите в папку infra:**
```bash
cd infra
```

**Запустите контейнеры:**
```bash
docker compose -f docker-compose.dev.yml up -d
```

**Что происходит:**
- `docker compose` - команда для управления несколькими контейнерами
- `-f docker-compose.dev.yml` - указать файл конфигурации
- `up` - запустить контейнеры
- `-d` - запустить в фоновом режиме (чтобы терминал был свободен)

**Вернитесь в корень проекта:**
```bash
cd ..
```

### Проверка, что контейнеры запущены:

```bash
docker ps
```

Вы должны увидеть 2 контейнера (postgres и redis) со статусом "Up".

---

## 🗄️ Шаг 3: Настройка базы данных

### Что мы делаем?
Создаем структуру таблиц в базе данных.

### Выполните команды:

**Перейдите в папку backend:**
```bash
cd backend
```

**Сгенерируйте Prisma клиент:**
```bash
pnpm prisma:generate
```

**Что это:** Prisma - это инструмент для работы с базой данных. Эта команда создает код для работы с БД.

**Примените схему к базе данных:**
```bash
pnpm prisma:push
```

**Что это:** Создает таблицы в базе данных согласно схеме.

**Вернитесь в корень проекта:**
```bash
cd ..
```

---

## 🎮 Шаг 4: Запуск приложения

Теперь нужно запустить 3 части приложения в отдельных терминалах.

### 📱 Вариант А: Использование скриптов (проще)

#### Способ 1: Автоматический запуск всех сервисов

В терминале выполните:
```bash
cd ~/Desktop/Memoraid\ 2
./start-all.sh
```

Это запустит все сервисы в фоновом режиме.

**Просмотр логов:**
```bash
tail -f logs/backend.log    # Логи backend
tail -f logs/frontend.log   # Логи frontend
tail -f logs/queue.log      # Логи queue
```

**Остановка всех сервисов:**
```bash
./stop-all.sh
```

#### Способ 2: Подготовка окружения + ручной запуск

Выполните подготовку:
```bash
cd ~/Desktop/Memoraid\ 2
./start-dev.sh
```

Затем запустите в 3 терминалах (см. Вариант Б ниже).

---

### 📱 Вариант Б: Ручной запуск в 3 терминалах

#### Как открыть несколько терминалов:

**Способ 1:** Откройте Terminal 3 раза (Cmd+N в Terminal)

**Способ 2:** Используйте вкладки в Terminal:
- `Cmd+T` - новая вкладка
- `Cmd+W` - закрыть вкладку

---

#### Терминал 1: Backend (Серверная часть)

1. Откройте новый терминал
2. Перейдите в проект:
   ```bash
   cd ~/Desktop/Memoraid\ 2
   ```
3. Запустите backend:
   ```bash
   pnpm dev:backend
   ```

**Что вы увидите:**
- Сообщения о запуске сервера
- Адрес: `http://localhost:3001`
- Сообщения об ошибках (если есть)

**Не закрывайте этот терминал!** Оставьте его работать.

---

#### Терминал 2: Frontend (Интерфейс пользователя)

1. Откройте новый терминал (или новую вкладку)
2. Перейдите в проект:
   ```bash
   cd ~/Desktop/Memoraid\ 2
   ```
3. Запустите frontend:
   ```bash
   pnpm dev:frontend
   ```

**Что вы увидите:**
- Сообщения о компиляции
- Адрес: `http://localhost:3000`
- Сообщения об ошибках (если есть)

**Не закрывайте этот терминал!** Оставьте его работать.

---

#### Терминал 3: Queue Worker (Обработчик задач)

1. Откройте новый терминал (или новую вкладку)
2. Перейдите в backend:
   ```bash
   cd ~/Desktop/Memoraid\ 2/backend
   ```
3. Соберите проект:
   ```bash
   pnpm build
   ```
4. Запустите queue worker:
   ```bash
   pnpm queue:dev
   ```

**Что вы увидите:**
- Сообщения о запуске worker
- Сообщения об обработке задач
- Сообщения об ошибках (если есть)

**Не закрывайте этот терминал!** Оставьте его работать.

---

## ✅ Проверка, что все работает

### 1. Проверьте Docker контейнеры:
```bash
docker ps
```
Должно быть 2 контейнера (postgres и redis) со статусом "Up".

### 2. Проверьте, что сервисы запущены:

Откройте браузер и перейдите по адресам:

- **Frontend:** http://localhost:3000
  - Должна открыться главная страница приложения

- **Backend API:** http://localhost:3001/health
  - Должен вернуться ответ `{"status":"ok"}` или похожий

### 3. Проверьте терминалы:

Во всех 3 терминалах не должно быть красных ошибок. Если есть ошибки - скопируйте их и сообщите разработчику.

---

## 🛑 Как остановить проект

### Остановка сервисов:

В каждом из 3 терминалов нажмите:
```
Ctrl + C
```

Это остановит работу сервиса в этом терминале.

### Остановка Docker контейнеров:

```bash
cd ~/Desktop/Memoraid\ 2/infra
docker compose -f docker-compose.dev.yml down
cd ..
```

---

## 🔄 Как перезапустить проект

Если что-то сломалось, выполните:

1. Остановите все сервисы (Ctrl+C во всех терминалах)
2. Остановите Docker контейнеры (команда выше)
3. Запустите заново с Шага 2

---

## ❓ Частые проблемы и решения

### Проблема: "command not found: docker"
**Решение:** Установите Docker Desktop (см. Шаг 1)

### Проблема: "command not found: pnpm"
**Решение:** Установите pnpm:
```bash
npm install -g pnpm
```

### Проблема: "port already in use" (порт уже занят)
**Решение:** 
- Закройте другие приложения, использующие порты 3000, 3001, 5432, 6379
- Или перезагрузите компьютер

### Проблема: Docker контейнеры не запускаются
**Решение:**
- Убедитесь, что Docker Desktop запущен
- Проверьте логи: `cd infra && docker compose -f docker-compose.dev.yml logs`

### Проблема: "Permission denied" при запуске скриптов
**Решение:** Дайте права на выполнение:
```bash
chmod +x start-dev.sh start-all.sh stop-all.sh
```

---

## 📚 Полезные команды

### Просмотр логов Docker:
```bash
cd ~/Desktop/Memoraid\ 2/infra
docker compose -f docker-compose.dev.yml logs -f
```

### Просмотр запущенных контейнеров:
```bash
docker ps
```

### Просмотр всех контейнеров (включая остановленные):
```bash
docker ps -a
```

### Очистка неиспользуемых контейнеров:
```bash
docker system prune
```

---

## 🎓 Что означают команды

- `cd` - перейти в папку (change directory)
- `ls` - показать содержимое папки (list)
- `pwd` - показать текущую папку (print working directory)
- `pnpm` - менеджер пакетов (как npm, но быстрее)
- `docker compose` - управление несколькими контейнерами
- `up` - запустить
- `down` - остановить
- `-d` - фоновый режим (detached)
- `-f` - указать файл (file)

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте, что Docker Desktop запущен
2. Проверьте логи в терминалах
3. Убедитесь, что все команды выполнены правильно
4. Попробуйте перезапустить проект с начала

---

**Удачи в работе с проектом! 🚀**

