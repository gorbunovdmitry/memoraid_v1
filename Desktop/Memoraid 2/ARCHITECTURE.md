# 🏗️ Архитектура Memoraid - Как работает сервис

## 📊 Общая схема работы

```
┌─────────────────┐
│  Telegram App   │
│  (Frontend)     │
└────────┬────────┘
         │ HTTPS + initData (подпись Telegram)
         ▼
┌─────────────────┐
│   Backend API   │
│   (NestJS)       │
│                  │
│  ┌────────────┐ │
│  │ Middleware │ │ ← Проверяет подпись Telegram
│  │ (Auth)     │ │ ← Извлекает userId
│  └────────────┘ │
│                  │
│  ┌────────────┐ │
│  │ Services  │ │ ← Все запросы фильтруются по userId
│  └────────────┘ │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│Postgres│ │ Redis  │
│  (БД)  │ │(Очереди)│
└────────┘ └────────┘
```

---

## 🔐 Авторизация

### Как это работает:

1. **Пользователь открывает Mini App в Telegram**
   - Telegram автоматически передает `initData` во frontend
   - `initData` содержит информацию о пользователе и **криптографическую подпись**

2. **Frontend отправляет запрос на Backend**
   - Каждый запрос включает заголовок: `x-telegram-init-data`
   - В этом заголовке передается `initData` от Telegram

3. **Backend проверяет подпись**
   ```typescript
   // backend/src/common/user-context.middleware.ts
   - Проверяет подпись через HMAC-SHA256
   - Использует секретный ключ бота (TELEGRAM_BOT_TOKEN)
   - Если подпись неверна → 401 ошибка
   ```

4. **Backend извлекает userId**
   ```typescript
   - Из initData извлекается Telegram ID пользователя
   - Создается или находится пользователь в БД
   - userId добавляется в каждый запрос (req.userId)
   ```

### ✅ Ваше понимание: "Авторизация через Telegram (бесшовная)"
**Реальность:** ✅ **ПРАВИЛЬНО** - авторизация действительно через Telegram, бесшовная для пользователя

---

## 💾 Хранение данных

### Как это работает:

**Все данные хранятся на сервере в PostgreSQL**, а не локально!

### Структура базы данных:

```sql
User (пользователь)
  ├── id (BigInt) - внутренний ID
  ├── tgId (BigInt) - Telegram ID
  └── Связи:
      ├── memories[] - Заметки пользователя
      ├── events[] - События календаря
      ├── chats[] - Чаты пользователя
      └── ... другие данные

Memory (заметка)
  ├── userId (BigInt) - К какому пользователю относится
  ├── folderId (BigInt) - В какой папке
  ├── title (String) - Заголовок
  ├── content (String) - Содержимое
  └── embedding (vector) - Векторное представление для поиска

Event (событие)
  ├── userId (BigInt) - К какому пользователю относится
  ├── title (String) - Название события
  ├── startsAt (DateTime) - Когда начинается
  └── ...
```

### ❌ Ваше понимание: "Данные сохраняются локально (в Telegram или на устройстве)"
**Реальность:** ❌ **НЕПРАВИЛЬНО** - все данные хранятся на сервере в PostgreSQL

### ❌ Ваше понимание: "Данные в зашифрованных папках"
**Реальность:** ❌ **НЕПРАВИЛЬНО** - данные хранятся в открытом виде в БД (не зашифрованы)

---

## 🔍 Получение данных

### Как это работает:

1. **Пользователь делает запрос** (например, открывает список заметок)
2. **Frontend отправляет запрос на Backend** с `initData`
3. **Backend проверяет авторизацию** и извлекает `userId`
4. **Backend фильтрует данные по userId**:
   ```typescript
   // backend/src/modules/memory/memory.service.ts
   const memories = await prisma.memory.findMany({
     where: { userId: req.userId } // ← Только данные этого пользователя!
   });
   ```
5. **Backend возвращает данные** пользователю

### ✅ Ваше понимание: "При запросах подгружает данные с локальной БД"
**Реальность:** ✅ **ЧАСТИЧНО ПРАВИЛЬНО** - данные подгружаются, но **с сервера**, а не локально

---

## 🛡️ Безопасность

### Как обеспечивается изоляция данных:

1. **Каждый запрос проверяет авторизацию**
   - Middleware проверяет подпись Telegram
   - Если подпись неверна → 401 Unauthorized

2. **Все запросы фильтруются по userId**
   ```typescript
   // Пример из memory.service.ts
   async findAll(userId: bigint) {
     return this.prisma.memory.findMany({
       where: { userId } // ← Только данные этого пользователя
     });
   }
   ```

3. **Пользователь не может получить данные другого пользователя**
   - `userId` извлекается из проверенной подписи Telegram
   - Невозможно подделать `userId` без знания секретного ключа бота

### ✅ Ваше понимание: "Неавторизованный пользователь не может получить данные"
**Реальность:** ✅ **ПРАВИЛЬНО** - без валидной подписи Telegram запросы отклоняются

### ✅ Ваше понимание: "Авторизованный пользователь не может получить данные другого"
**Реальность:** ✅ **ПРАВИЛЬНО** - все запросы фильтруются по `userId` из подписи

---

## 📱 Поток данных (пример: сохранение заметки)

```
1. Пользователь в Telegram:
   "Запомни, что я люблю фильмы ужасов"
   
2. Frontend (Telegram Mini App):
   - Получает текст от пользователя
   - Отправляет POST /ingest с:
     * text: "Запомни, что я люблю фильмы ужасов"
     * x-telegram-init-data: "user=...&hash=..." (подпись Telegram)
   
3. Backend (Middleware):
   - Проверяет подпись initData
   - Извлекает Telegram ID → userId
   - Добавляет userId в запрос
   
4. Backend (IngestService):
   - Классифицирует запрос через AI (Gemini)
   - Определяет: kind="memory", folder="Хобби и проекты"
   
5. Backend (MemoryService):
   - Создает запись в БД:
     INSERT INTO Memory (userId, folderId, title, content, ...)
     VALUES (userId, folderId, "я люблю фильмы ужасов", ...)
   - Генерирует embedding для семантического поиска
   
6. Backend возвращает ответ:
   { success: true, id: "123", ... }
   
7. Frontend показывает подтверждение пользователю
```

---

## 🔄 Поток данных (пример: получение заметок)

```
1. Пользователь открывает раздел "Memories"
   
2. Frontend:
   - Отправляет GET /memories?folder=...
   - С заголовком: x-telegram-init-data
   
3. Backend (Middleware):
   - Проверяет подпись
   - Извлекает userId
   
4. Backend (MemoryService):
   - Выполняет запрос:
     SELECT * FROM Memory 
     WHERE userId = $userId AND folderId = $folderId
   - Возвращает только заметки этого пользователя
   
5. Frontend отображает список заметок
```

---

## 🆚 Сравнение: Ваше понимание vs Реальность

| Аспект | Ваше понимание | Реальность |
|--------|----------------|------------|
| **Авторизация** | ✅ Через Telegram (бесшовная) | ✅ **ПРАВИЛЬНО** |
| **Хранение данных** | ❌ Локально (в Telegram/на устройстве) | ❌ **На сервере в PostgreSQL** |
| **Шифрование** | ❌ Данные в зашифрованных папках | ❌ **Данные в открытом виде** |
| **Подгрузка данных** | ✅ С локальной БД | ⚠️ **С сервера, но фильтруется по userId** |
| **Неавторизованный доступ** | ✅ Не может получить данные | ✅ **ПРАВИЛЬНО** |
| **Изоляция пользователей** | ✅ Не может получить данные другого | ✅ **ПРАВИЛЬНО** |

---

## 🔑 Ключевые отличия

### 1. **Данные на сервере, а не локально**
- ❌ Не хранятся в Telegram
- ❌ Не хранятся на устройстве пользователя
- ✅ Хранятся в PostgreSQL на вашем сервере

### 2. **Данные не зашифрованы**
- ❌ Нет шифрования на уровне БД
- ✅ Данные хранятся в открытом виде
- ⚠️ Защита обеспечивается через авторизацию и фильтрацию по userId

### 3. **Интернет обязателен**
- ❌ Не работает офлайн
- ✅ Требуется подключение к интернету для всех операций

### 4. **Централизованное хранение**
- ✅ Все данные в одном месте (ваш сервер)
- ✅ Можно делать резервные копии
- ✅ Можно анализировать данные
- ⚠️ Вы несете ответственность за безопасность данных

---

## 💡 Почему так сделано?

### Преимущества текущей архитектуры:

1. **Семантический поиск**
   - Векторные embeddings требуют вычислений на сервере
   - Локально это было бы невозможно

2. **AI функции**
   - Генерация советов на основе всех данных пользователя
   - Требует доступа ко всем данным пользователя

3. **Синхронизация**
   - Данные доступны с любого устройства
   - Нет проблем с синхронизацией

4. **Простота разработки**
   - Не нужно управлять локальным хранилищем
   - Не нужно шифровать/расшифровывать данные

### Недостатки:

1. **Требуется интернет**
   - Не работает офлайн

2. **Зависимость от сервера**
   - Если сервер упал - приложение не работает

3. **Безопасность**
   - Данные на сервере могут быть скомпрометированы
   - Нет шифрования на уровне БД

---

## 🔒 Рекомендации по безопасности

Если хотите улучшить безопасность:

1. **Шифрование данных в БД**
   - Использовать поле-уровневое шифрование
   - Шифровать sensitive данные (например, медицинские записи)

2. **HTTPS обязательно**
   - Все запросы должны идти через HTTPS
   - Telegram требует HTTPS для Mini Apps

3. **Резервное копирование**
   - Регулярные бэкапы БД
   - Хранение бэкапов в зашифрованном виде

4. **Мониторинг**
   - Отслеживание подозрительной активности
   - Логирование всех запросов

---

## 📝 Итог

**Ваше понимание:** Приложение работает как локальное хранилище с синхронизацией через Telegram.

**Реальность:** Приложение работает как **облачный сервис**, где:
- ✅ Авторизация через Telegram (правильно)
- ✅ Данные хранятся на сервере (не локально)
- ✅ Данные не зашифрованы (хранятся в открытом виде)
- ✅ Безопасность обеспечивается через авторизацию и фильтрацию по userId
- ✅ Интернет обязателен для работы

Это классическая архитектура **SaaS (Software as a Service)** приложения.

