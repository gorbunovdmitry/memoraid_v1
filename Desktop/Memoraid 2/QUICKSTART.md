# ⚡ Быстрый старт Memoraid

## ✅ Что уже сделано:

1. ✅ Установлены все зависимости
2. ✅ Созданы файлы `.env` с настройками
3. ✅ Проект готов к запуску

## 🚀 Что нужно сделать дальше:

### 1. Установите Docker Desktop (если еще не установлен)

Скачайте и установите с [docker.com](https://www.docker.com/products/docker-desktop)

После установки запустите Docker Desktop и дождитесь его полной загрузки.

### 2. Запустите Docker контейнеры

Откройте терминал в папке проекта и выполните:

```bash
cd infra
docker compose -f docker-compose.dev.yml up -d
cd ..
```

Это запустит:
- PostgreSQL с pgvector (порт 5432)
- Redis (порт 6379)

### 3. Настройте базу данных

```bash
cd backend
pnpm prisma:generate
pnpm prisma:push
cd ..
```

### 4. Запустите проект

Откройте **3 терминала**:

#### Терминал 1 - Backend:
```bash
pnpm dev:backend
```

#### Терминал 2 - Frontend:
```bash
pnpm dev:frontend
```

#### Терминал 3 - Queue Worker:
```bash
cd backend
pnpm build
pnpm queue:dev
```

### 5. Откройте приложение

- Frontend: http://localhost:3000
- Backend API: http://localhost:3001
- Health check: http://localhost:3001/health

## 📝 Полезные команды

### Остановить Docker контейнеры:
```bash
cd infra
docker compose -f docker-compose.dev.yml down
```

### Просмотр логов Docker:
```bash
cd infra
docker compose -f docker-compose.dev.yml logs -f
```

### Перезапустить базу данных:
```bash
cd infra
docker compose -f docker-compose.dev.yml down
docker compose -f docker-compose.dev.yml up -d
cd ../backend
pnpm prisma:push
```

## ❓ Проблемы?

Если что-то не работает:

1. Убедитесь что Docker Desktop запущен
2. Проверьте что порты 3000, 3001, 5432, 6379 свободны
3. Проверьте логи в терминалах где запущены сервисы

Подробная документация: `SETUP.md`

