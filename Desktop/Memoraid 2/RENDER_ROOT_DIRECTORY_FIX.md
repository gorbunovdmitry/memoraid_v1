# 🔧 Исправление ошибки "Root directory backend does not exist" в Render

## ❌ Проблема

При деплое Backend сервиса в Render возникает ошибка:
```
Root directory "backend" does not exist
cd: /opt/render/project/src/backend: No such file or directory
```

## ✅ Решение

### Вариант 1: Проверьте Root Directory в настройках Render

1. Откройте ваш Backend сервис в Render Dashboard
2. Перейдите в **"Settings"**
3. Найдите раздел **"Build & Deploy"**
4. Проверьте поле **"Root Directory"**

**Правильное значение:**
```
backend
```

**Неправильные значения (не используйте):**
- `/backend`
- `./backend`
- `backend/`
- `Desktop/Memoraid 2/backend`

### Вариант 2: Убедитесь, что файлы в Git

Проверьте, что директория `backend` действительно в репозитории:

```bash
git ls-files | grep "^backend/"
```

Должны быть файлы типа:
- `backend/package.json`
- `backend/src/...`
- и т.д.

Если файлов нет, добавьте их:

```bash
git add backend/
git commit -m "Add backend directory"
git push origin main
```

### Вариант 3: Очистите Root Directory (если не помогает)

1. В Render Settings → **"Build & Deploy"**
2. **Очистите** поле "Root Directory" (оставьте пустым)
3. Измените Build Command на:
   ```bash
   cd backend && pnpm install && pnpm prisma generate && pnpm build
   ```
4. Измените Start Command на:
   ```bash
   cd backend && node dist/main.js
   ```

### Вариант 4: Проверьте структуру репозитория

Убедитесь, что в корне репозитория есть:
- `backend/` (директория)
- `frontend/` (директория)
- `package.json` (корневой)
- `pnpm-workspace.yaml`

Если структура другая, возможно файлы были закоммичены с неправильным путем.

---

## 🔍 Диагностика

### Проверка 1: Структура в Git
```bash
git ls-tree -d HEAD | grep backend
```

Должно показать:
```
040000 tree ...    backend
```

### Проверка 2: Файлы в backend
```bash
git ls-files backend/ | head -5
```

Должны быть файлы из директории backend.

### Проверка 3: Локальная структура
```bash
ls -la | grep backend
```

Должна быть директория `backend`.

---

## ✅ Правильные настройки для Render Backend

**Root Directory:**
```
backend
```

**Build Command:**
```bash
pnpm install && pnpm prisma generate && pnpm build
```

**Start Command:**
```bash
node dist/main.js
```

---

## 🚨 Если ничего не помогает

1. **Удалите** Backend сервис в Render
2. **Пересоздайте** его заново
3. Убедитесь, что:
   - Репозиторий подключен правильно
   - Branch: `main`
   - Root Directory: `backend` (без слешей)
   - Все команды правильные

---

## 📝 Примечание

Если файлы были закоммичены с полным путем (например, `Desktop/Memoraid 2/backend/...`), нужно их перекоммитить правильно:

```bash
# Удалите старые файлы из Git (но не с диска!)
git rm -r --cached backend/
git add backend/
git commit -m "Fix backend directory structure"
git push origin main
```

