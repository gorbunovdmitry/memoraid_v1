# 📝 Пошаговая инструкция создания PostgreSQL и Redis в Render

## 🗄️ Шаг 1: Создание PostgreSQL

### 1.1 Откройте Dashboard Render
1. Войдите на [render.com](https://render.com)
2. Вы попадете на главную страницу Dashboard

### 1.2 Создайте PostgreSQL
1. Нажмите кнопку **"+ New"** (вверху справа или в центре страницы)
2. В выпадающем меню выберите **"PostgreSQL"**

### 1.3 Заполните форму:

**Name:**
```
memoraid-db
```

**Database:**
```
memoraid
```
(или оставьте значение по умолчанию)

**User:**
```
memoraid
```
(или оставьте значение по умолчанию)

**Region:**
Выберите ближайший регион:
- `Oregon (US West)` - для США
- `Frankfurt (EU Central)` - для Европы
- `Singapore (AP Southeast)` - для Азии

**PostgreSQL Version:**
```
15
```
(или новее, если доступно)

**Plan:**
- `Free` - для тестирования (ограничения по размеру БД)
- `Starter` ($7/мес) - для продакшена (рекомендуется)

### 1.4 Сохраните PostgreSQL
1. Нажмите кнопку **"Create Database"**
2. Render начнет создавать БД (займет 1-2 минуты)

### 1.5 Получите DATABASE_URL
После создания:
1. Откройте созданный PostgreSQL сервис (кликните на него)
2. Перейдите в раздел **"Info"** или **"Connections"**
3. Найдите поле **"Internal Database URL"** или **"Connection String"**
4. **Скопируйте** значение - это ваш `DATABASE_URL`

Пример:
```
postgresql://memoraid:password@dpg-xxxxx-a.frankfurt-postgres.render.com/memoraid
```

### 1.6 Установите расширение pgvector
После создания БД нужно установить расширение `pgvector`:

**Вариант A: Через Render Shell (рекомендуется)**
1. Откройте PostgreSQL сервис
2. Перейдите в раздел **"Shell"** или **"Connect"**
3. В открывшемся терминале выполните:
```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

**Вариант B: Через psql (локально)**
1. В PostgreSQL сервисе найдите **"External Connection"**
2. Скопируйте строку подключения
3. В терминале выполните:
```bash
psql "postgresql://user:password@host:port/dbname"
```
4. В psql выполните:
```sql
CREATE EXTENSION IF NOT EXISTS vector;
\q
```

---

## 🔴 Шаг 2: Создание Key Value (Redis)

**Важно:** В Render Redis заменен на "Key Value" (Valkey), который полностью совместим с Redis. Ваш код будет работать без изменений!

### 2.1 Создайте Key Value
1. В Dashboard нажмите **"+ New"**
2. Выберите **"Key Value"** (не Redis!)

### 2.2 Заполните форму:

**Name:**
```
memoraid-keyvalue
```

**Region:**
Выберите **тот же регион**, что и для PostgreSQL (важно для скорости!)

**Plan:**
- `Free` - для тестирования (ограничения по памяти)
- `Starter` ($7/мес) - для продакшена (рекомендуется)

### 2.3 Сохраните Key Value
1. Нажмите кнопку **"Create Key Value"**
2. Render начнет создавать сервис (займет 1-2 минуты)

### 2.4 Получите REDIS_URL
После создания:
1. Откройте созданный Key Value сервис
2. Перейдите в раздел **"Info"** или **"Connections"**
3. Найдите поле **"Internal Key Value URL"** или **"Connection String"**
4. **Скопируйте** значение - это ваш `REDIS_URL`

Пример:
```
rediss://default:password@dpg-xxxxx-a.frankfurt-redis.render.com:6379
```

**Примечание:** URL будет выглядеть как Redis URL и полностью совместим с вашим кодом!

---

## ✅ Проверка

После создания обоих сервисов у вас должны быть:

- ✅ PostgreSQL сервис `memoraid-db`
- ✅ Redis сервис `memoraid-redis`
- ✅ `DATABASE_URL` (скопирован)
- ✅ `REDIS_URL` (скопирован)
- ✅ Расширение `pgvector` установлено в PostgreSQL

---

## 🚀 Следующий шаг

Теперь можно создавать Backend сервис и использовать эти переменные в Environment Variables!

---

## 📸 Визуальная подсказка

В Render Dashboard:
- **"+ New"** находится в правом верхнем углу или в центре страницы
- После клика появится меню с опциями: `Web Service`, `Static Site`, `Background Worker`, `PostgreSQL`, `Redis`, и т.д.
- Выберите нужный тип сервиса и заполните форму

