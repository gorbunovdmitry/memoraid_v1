# 🔍 Можно ли пропустить pgvector на данном этапе?

## ✅ ДА, можно пропустить!

### Что будет работать БЕЗ pgvector:

- ✅ Создание заметок (memories)
- ✅ Создание событий календаря
- ✅ Обычный поиск по ключевым словам
- ✅ Генерация советов (advice)
- ✅ Чат с AI
- ✅ Все основные функции приложения

### Что НЕ будет работать БЕЗ pgvector:

- ❌ Семантический поиск (поиск по смыслу)
- ⚠️ Генерация советов может быть менее точной (но все равно будет работать)

---

## 🔧 Как временно отключить pgvector

### Вариант 1: Использовать `prisma db push` без pgvector

Измените Build Command в Render на:
```bash
pnpm install --prod=false && pnpm exec prisma generate --schema=./prisma/schema.prisma && pnpm exec prisma db push --schema=./prisma/schema.prisma --accept-data-loss --skip-generate && pnpm build
```

Но сначала нужно изменить схему Prisma, чтобы убрать `vector` тип.

### Вариант 2: Просто пропустить установку pgvector

Используйте текущий Build Command с `prisma db push`:
```bash
pnpm install --prod=false && pnpm exec prisma generate --schema=./prisma/schema.prisma && pnpm exec prisma db push --schema=./prisma/schema.prisma --accept-data-loss && pnpm build
```

Prisma создаст таблицы, но колонка `embedding` будет иметь тип `text` вместо `vector`. Это нормально для начала.

---

## 📝 Рекомендация

**Просто используйте `prisma db push` без установки pgvector.**

Таблицы создадутся, приложение будет работать. Семантический поиск можно добавить позже, когда будете готовы установить pgvector.

---

## 🚀 Что делать сейчас

1. Используйте Build Command с `prisma db push` (уже обновлено в инструкции)
2. Пропустите установку pgvector
3. Создайте Frontend сервис
4. Настройте Telegram Mini App

Все будет работать! pgvector можно добавить позже.

