# 🔧 Исправление ошибки миграции

## ❌ Проблема

Ошибка: `ERROR: relation "memories" does not exist`

Причина: Миграция `0001_init` пытается изменить таблицу `memories`, которая еще не создана. Файл миграции содержит только команды для pgvector, но не создает таблицы.

## ✅ Решение

### Вариант 1: Использовать `prisma db push` (рекомендуется для первого деплоя)

`prisma db push` создает схему напрямую из `schema.prisma` без необходимости в миграциях.

**Build Command:**
```bash
pnpm install --prod=false && pnpm exec prisma generate --schema=./prisma/schema.prisma && pnpm exec prisma db push --schema=./prisma/schema.prisma --accept-data-loss && pnpm build
```

**Важно:** `--accept-data-loss` нужен, если нужно пересоздать таблицы.

---

### Вариант 2: Исправить миграцию (если хотите использовать миграции)

Нужно создать правильную миграцию, которая сначала создает таблицы, а потом изменяет их.

Но для первого деплоя проще использовать `prisma db push`.

---

### Вариант 3: Создать новую миграцию

1. Удалите старую миграцию (или оставьте)
2. Создайте новую миграцию:
```bash
cd backend
pnpm prisma migrate dev --name init
```

Но это требует подключения к БД, что сложно на Render.

---

## 🎯 Рекомендация

Используйте **Вариант 1** с `prisma db push` - это самый простой способ для первого деплоя.

После первого успешного деплоя можно переключиться на миграции для будущих изменений схемы.

---

## 📝 Итоговый Build Command для Render:

```bash
pnpm install --prod=false && pnpm exec prisma generate --schema=./prisma/schema.prisma && pnpm exec prisma db push --schema=./prisma/schema.prisma --accept-data-loss && pnpm build
```

---

## ⚠️ Примечание

`prisma db push` создает схему напрямую, но не отслеживает историю миграций. Для продакшена лучше использовать миграции, но для первого деплоя `db push` - самый простой вариант.

