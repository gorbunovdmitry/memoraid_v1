# 🔧 Исправление ошибки "missing initData or botToken"

## 🔍 Диагностика проблемы

Ошибка возникает, когда бэкенд не может получить `initData` от Telegram или `TELEGRAM_BOT_TOKEN` не установлен.

---

## ✅ Шаг 1: Проверить TELEGRAM_BOT_TOKEN в Backend

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Найдите ваш **Backend сервис** (например, `memoraid-backend`)
3. Перейдите в раздел **"Environment"**
4. Проверьте, есть ли переменная `TELEGRAM_BOT_TOKEN`
5. Если её нет или она пустая:
   - Нажмите **"Add Environment Variable"**
   - **Key**: `TELEGRAM_BOT_TOKEN`
   - **Value**: Ваш токен бота от BotFather (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
   - Нажмите **"Save Changes"**
   - Render автоматически перезапустит сервис

**Как получить токен бота:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"API Token"**
5. Скопируйте токен

---

## ✅ Шаг 2: Проверить логи Backend

После добавления `TELEGRAM_BOT_TOKEN` проверьте логи:

1. В Render Dashboard откройте Backend сервис
2. Перейдите в раздел **"Logs"**
3. Попробуйте отправить сообщение в Mini App
4. Найдите в логах строки:
   ```
   [UserContextMiddleware] initData header: present/missing
   [UserContextMiddleware] botToken: SET/NOT SET
   ```

**Если видите:**
- `botToken: NOT SET` → `TELEGRAM_BOT_TOKEN` не установлен (см. Шаг 1)
- `initData header: missing` → `initData` не передается с фронтенда (см. Шаг 3)

---

## ✅ Шаг 3: Проверить initData на Frontend

### Вариант A: Проверить в консоли браузера

1. Откройте Mini App в Telegram
2. Откройте консоль разработчика (если возможно)
3. Попробуйте отправить сообщение
4. Найдите в консоли:
   ```
   [useTelegram] WebApp initialized, initData: present/missing
   [handleSendMessage] initData: present/missing
   ```

### Вариант B: Проверить логи Frontend в Render

1. В Render Dashboard откройте Frontend сервис
2. Перейдите в раздел **"Logs"**
3. Попробуйте отправить сообщение в Mini App
4. Найдите в логах строки с `[handleSendMessage]` или `[useTelegram]`

---

## 🔧 Решение проблем

### Проблема 1: `initData` отсутствует в Telegram Mini App

**Причина:** Mini App может не передавать `initData`, если:
- Mini App открыт не через Telegram (например, в браузере)
- URL Mini App настроен неправильно в BotFather
- Telegram WebApp SDK не инициализирован правильно

**Решение:**
1. Убедитесь, что Mini App открыт **внутри Telegram** (не в браузере)
2. Проверьте, что URL в BotFather правильный и начинается с `https://`
3. Убедитесь, что Frontend сервис работает и доступен

### Проблема 2: `initData` есть, но не передается на бэкенд

**Причина:** Проблема с передачей заголовка `x-telegram-init-data`

**Решение:**
1. Проверьте логи Frontend - должно быть `[handleSendMessage] initData: present`
2. Если `initData` есть на фронтенде, но не доходит до бэкенда, проверьте:
   - Правильность URL бэкенда в `NEXT_PUBLIC_BACKEND_URL`
   - Что запрос действительно отправляется на правильный endpoint

### Проблема 3: `TELEGRAM_BOT_TOKEN` не установлен

**Решение:**
1. См. Шаг 1 выше
2. После добавления токена подождите 1-2 минуты, пока сервис перезапустится
3. Попробуйте отправить сообщение снова

---

## 🧪 Тестирование после исправления

После исправления проблем:

1. Откройте Mini App в Telegram
2. Отправьте тестовое сообщение (например, "привет")
3. Проверьте логи Backend:
   - Должно быть: `[UserContextMiddleware] initData header: present`
   - Должно быть: `[UserContextMiddleware] botToken: SET`
   - Должно быть: `[ingest] Received request: userId=...`
4. Если все логи правильные, но ошибка остается - проверьте логику валидации `initData`

---

## 📝 Чеклист

- [ ] `TELEGRAM_BOT_TOKEN` установлен в переменных окружения Backend
- [ ] Backend сервис перезапущен после добавления токена
- [ ] В логах Backend видно `botToken: SET`
- [ ] В логах Frontend видно `initData: present`
- [ ] В логах Backend видно `initData header: present`
- [ ] Сообщения отправляются успешно

---

## 🆘 Если проблема не решена

Если после выполнения всех шагов проблема остается:

1. **Проверьте логи Backend** - там должна быть подробная информация о том, что именно отсутствует
2. **Проверьте логи Frontend** - там должна быть информация о том, передается ли `initData`
3. **Убедитесь, что Mini App открыт в Telegram**, а не в браузере
4. **Проверьте, что URL Frontend правильный** в настройках BotFather

---

## 💡 Дополнительная информация

### Что такое initData?

`initData` - это строка, которую Telegram передает в Mini App при его открытии. Она содержит информацию о пользователе и подписана секретным ключом бота. Бэкенд использует её для:
- Проверки, что запрос действительно пришел из Telegram
- Получения информации о пользователе (ID, имя и т.д.)
- Авторизации пользователя

### Почему это важно?

Без `initData` и `TELEGRAM_BOT_TOKEN` бэкенд не может:
- Проверить подлинность запроса
- Определить, какой пользователь отправляет сообщение
- Защитить API от несанкционированного доступа

