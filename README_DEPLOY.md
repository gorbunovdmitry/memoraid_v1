# 🚀 Деплой Memoraid в продакшен

## 📚 Документация по деплою

В проекте есть две инструкции для деплоя:

1. **`QUICK_DEPLOY.md`** - Быстрая инструкция для опытных разработчиков
2. **`DEPLOY.md`** - Подробная пошаговая инструкция со всеми деталями

## 🎯 Что нужно перед деплоем

- ✅ Telegram Bot Token (от @BotFather)
- ✅ Gemini API Key
- ✅ Сервер в Yandex Cloud (или другой облачный провайдер)
- ✅ Домен (опционально, можно использовать IP)

## 📦 Что было создано

### Docker файлы:
- `backend/Dockerfile` - образ для backend (NestJS)
- `frontend/Dockerfile` - образ для frontend (Next.js)
- `infra/docker-compose.prod.yml` - конфигурация для продакшена
- `.dockerignore` - исключения для Docker build

### Скрипты:
- `infra/deploy.sh` - автоматический скрипт деплоя
- `infra/.env.prod.example` - пример файла с переменными окружения

### Документация:
- `DEPLOY.md` - полная инструкция
- `QUICK_DEPLOY.md` - быстрый старт

## 🚀 Быстрый старт

```bash
# 1. Подготовьте Telegram Bot в @BotFather
# 2. Создайте сервер в Yandex Cloud
# 3. Подключитесь к серверу
ssh ubuntu@YOUR_IP

# 4. Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh && sudo sh get-docker.sh

# 5. Клонируйте проект
git clone YOUR_REPO_URL memoraid
cd memoraid/infra

# 6. Настройте переменные окружения
cp .env.prod.example .env.prod
nano .env.prod  # Заполните все переменные

# 7. Запустите деплой
./deploy.sh
```

## ⚙️ Переменные окружения

Создайте файл `infra/.env.prod` на основе `infra/.env.prod.example`:

```env
# Обязательные
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
GEMINI_API_KEY=ваш_ключ_Gemini
POSTGRES_PASSWORD=надежный_пароль

# URL (замените на ваш домен)
NEXT_PUBLIC_BACKEND_URL=https://your-domain.com/api
```

## 📱 Настройка Telegram Mini App

После деплоя:

1. Откройте [@BotFather](https://t.me/BotFather)
2. `/myapps` → выберите вашего бота
3. `/editapp` → Web App URL
4. Введите: `https://your-domain.com`

## 🔍 Проверка работы

```bash
# Логи
docker-compose -f docker-compose.prod.yml logs -f

# Статус
docker-compose -f docker-compose.prod.yml ps

# Перезапуск
docker-compose -f docker-compose.prod.yml restart
```

## 🆘 Проблемы?

См. раздел "Решение проблем" в `DEPLOY.md`

