# 🤖 Как совместить AI функции с локальным хранением

## 🎯 Проблема

AI функции требуют:
- ✅ Доступ ко **всем** данным пользователя
- ✅ Вычисления на сервере (векторные embeddings)
- ✅ Генерация советов на основе истории

Локальное хранение означает:
- ✅ Данные на устройстве пользователя
- ✅ Шифрование end-to-end
- ❌ Сервер не видит данные

**Как совместить?**

---

## 💡 Решения

### 1. 🔄 Гибридный подход: Локальное + Синхронизация для AI

**Идея:** Данные хранятся локально, но **синхронизируются с сервером для AI функций**.

#### Архитектура:

```
┌─────────────────┐
│  Локальное      │
│  хранилище      │
│  (IndexedDB)    │
└────────┬────────┘
         │
         │ Синхронизация (зашифровано)
         ▼
┌─────────────────┐
│   Сервер        │
│   (для AI)      │
│                 │
│  ┌───────────┐ │
│  │ Векторы   │ │ ← Только embeddings (не сами данные)
│  │ AI модель │ │
│  └───────────┘ │
└─────────────────┘
```

#### Как это работает:

1. **Пользователь создает заметку**
   ```typescript
   // Локально (на устройстве)
   const memory = {
     id: generateId(),
     title: "Я люблю фильмы ужасов",
     content: "...",
     encrypted: false // Хранится локально в открытом виде
   };
   
   // Сохраняем локально
   await localDB.memories.put(memory);
   
   // Генерируем embedding локально или на сервере
   const embedding = await generateEmbedding(memory.title + memory.content);
   
   // Отправляем на сервер ТОЛЬКО embedding (без текста)
   await syncToServer({
     memoryId: memory.id,
     embedding: embedding, // Векторное представление
     folder: "Хобби",
     // НЕ отправляем title и content!
   });
   ```

2. **Сервер хранит только embeddings**
   ```typescript
   // На сервере
   {
     userId: 123,
     memoryId: "abc",
     embedding: [0.1, 0.2, ...], // Вектор
     folder: "Хобби",
     // НЕТ title и content!
   }
   ```

3. **AI функции работают с embeddings**
   ```typescript
   // Пользователь: "Посоветуй фильм"
   
   // 1. Генерируем embedding запроса локально
   const queryEmbedding = await generateEmbedding("Посоветуй фильм");
   
   // 2. Отправляем на сервер
   const similarMemories = await server.findSimilar(queryEmbedding);
   // Сервер возвращает только memoryId и folder (без текста!)
   
   // 3. Получаем полные данные локально
   const memories = await localDB.memories.bulkGet(similarMemories.map(m => m.memoryId));
   
   // 4. Формируем контекст локально
   const context = memories.map(m => `${m.title}: ${m.content}`).join('\n');
   
   // 5. Отправляем на сервер ТОЛЬКО контекст (без userId)
   const advice = await server.generateAdvice(context);
   ```

#### ✅ Преимущества:
- Данные остаются локально (зашифрованы)
- Сервер не видит содержимое заметок
- AI функции работают через embeddings
- Можно использовать локальные AI модели

#### ❌ Недостатки:
- Сложнее реализовать
- Нужна синхронизация
- Embeddings могут раскрыть информацию

---

### 2. 🧠 Локальные AI модели (Ollama, TensorFlow.js)

**Идея:** Использовать AI модели, которые работают **полностью на устройстве**.

#### Архитектура:

```
┌─────────────────┐
│  Устройство     │
│                 │
│  ┌───────────┐ │
│  │ Локальная │ │
│  │ БД        │ │
│  └───────────┘ │
│                 │
│  ┌───────────┐ │
│  │ AI модель │ │ ← Работает локально
│  │ (Ollama)  │ │
│  └───────────┘ │
└─────────────────┘
```

#### Пример с Ollama:

```typescript
// Установка Ollama локально
// npm install ollama

import { Ollama } from 'ollama';

const ollama = new Ollama({
  host: 'http://localhost:11434' // Локальный сервер Ollama
});

// Генерация советов локально
async function generateAdvice(userQuery: string) {
  // Получаем все заметки локально
  const memories = await localDB.memories.toArray();
  
  // Формируем контекст
  const context = memories
    .map(m => `${m.title}: ${m.content}`)
    .join('\n');
  
  // Генерируем совет через локальную модель
  const response = await ollama.generate({
    model: 'llama2', // или другая модель
    prompt: `На основе этих данных: ${context}\n\nВопрос: ${userQuery}\n\nОтвет:`
  });
  
  return response.text;
}

// Семантический поиск локально
async function semanticSearch(query: string) {
  // Используем локальную модель для генерации embedding
  const queryEmbedding = await generateEmbeddingLocally(query);
  
  // Ищем похожие локально
  const memories = await localDB.memories.toArray();
  const similarities = memories.map(m => ({
    memory: m,
    similarity: cosineSimilarity(queryEmbedding, m.embedding)
  }));
  
  return similarities
    .sort((a, b) => b.similarity - a.similarity)
    .slice(0, 10);
}
```

#### ✅ Преимущества:
- Полная приватность (данные не покидают устройство)
- Работает офлайн
- Нет зависимости от сервера
- Соответствие GDPR

#### ❌ Недостатки:
- Требует мощное устройство
- Модели занимают много места (несколько GB)
- Медленнее, чем облачные модели
- Нужно управлять моделями на устройстве

---

### 3. 🔐 Шифрование на клиенте перед отправкой

**Идея:** Данные шифруются на клиенте, сервер работает с зашифрованными данными.

#### Архитектура:

```
┌─────────────────┐
│  Клиент         │
│                 │
│  ┌───────────┐ │
│  │ Шифрование│ │ ← Шифруем перед отправкой
│  └───────────┘ │
└────────┬────────┘
         │ Зашифрованные данные
         ▼
┌─────────────────┐
│   Сервер        │
│                 │
│  ┌───────────┐ │
│  │ AI модель │ │ ← Работает с зашифрованными данными
│  └───────────┘ │
└─────────────────┘
```

#### Проблема:
❌ **AI модели не могут работать с зашифрованными данными!**

Шифрование делает данные случайными, AI не может их понять.

#### Решение: Homomorphic Encryption (очень сложно)
- Позволяет вычислять над зашифрованными данными
- Очень медленно
- Сложно реализовать
- Не подходит для production

---

### 4. 🔄 Federated Learning подход

**Идея:** AI модель обучается локально, обновления отправляются на сервер.

#### Архитектура:

```
┌─────────────────┐     ┌─────────────────┐
│  Устройство 1   │     │  Устройство 2   │
│                 │     │                 │
│  Локальная БД   │     │  Локальная БД   │
│  AI модель      │     │  AI модель      │
└────────┬────────┘     └────────┬────────┘
         │                      │
         │ Обновления модели    │
         └──────────┬───────────┘
                    ▼
         ┌─────────────────┐
         │   Сервер        │
         │                 │
         │ Агрегирует      │
         │ обновления      │
         └─────────────────┘
```

#### Как это работает:

1. **Каждое устройство имеет локальную модель**
2. **Модель обучается на локальных данных**
3. **Только обновления модели отправляются на сервер** (не данные!)
4. **Сервер агрегирует обновления**
5. **Обновленная модель рассылается всем устройствам**

#### ✅ Преимущества:
- Данные остаются локально
- Модель улучшается на основе всех пользователей
- Приватность сохраняется

#### ❌ Недостатки:
- Очень сложно реализовать
- Требует много ресурсов на устройстве
- Медленное обучение
- Не подходит для вашего случая (нужны персональные советы)

---

## 🏆 Рекомендуемое решение для вашего проекта

### Гибридный подход: Локальное хранение + Embeddings на сервере

#### Как это работает:

1. **Данные хранятся локально** (IndexedDB)
2. **Embeddings генерируются и хранятся на сервере** (без текста)
3. **AI функции работают через embeddings**
4. **Контекст формируется локально** перед отправкой на AI

#### Пример реализации:

```typescript
// frontend/lib/localStorage.ts
export class LocalMemoryStorage {
  private db: IDBDatabase;
  
  async init() {
    // Инициализация IndexedDB
    this.db = await this.openDB();
  }
  
  async saveMemory(memory: Memory) {
    // Сохраняем локально
    await this.db.put('memories', memory);
    
    // Генерируем embedding локально или на сервере
    const embedding = await this.generateEmbedding(memory);
    
    // Отправляем на сервер ТОЛЬКО embedding
    await fetch('/api/memories/embeddings', {
      method: 'POST',
      body: JSON.stringify({
        memoryId: memory.id,
        embedding: embedding,
        folder: memory.folder
        // НЕ отправляем title и content!
      })
    });
  }
  
  async searchMemories(query: string) {
    // Генерируем embedding запроса
    const queryEmbedding = await this.generateEmbedding(query);
    
    // Ищем похожие embeddings на сервере
    const similarIds = await fetch('/api/memories/search', {
      method: 'POST',
      body: JSON.stringify({
        embedding: queryEmbedding
        // Сервер возвращает только memoryId
      })
    }).then(r => r.json());
    
    // Получаем полные данные локально
    const memories = await this.db.getAll('memories', similarIds);
    
    return memories;
  }
  
  async generateAdvice(query: string) {
    // Получаем релевантные заметки локально
    const relevantMemories = await this.searchMemories(query);
    
    // Формируем контекст локально
    const context = relevantMemories
      .map(m => `${m.title}: ${m.content}`)
      .join('\n');
    
    // Отправляем на сервер ТОЛЬКО контекст (без userId)
    const advice = await fetch('/api/advice', {
      method: 'POST',
      body: JSON.stringify({
        query: query,
        context: context
        // Сервер не знает, кому принадлежат данные
      })
    }).then(r => r.json());
    
    return advice;
  }
}
```

#### Backend (только embeddings):

```typescript
// backend/src/modules/memory/embedding.service.ts
@Injectable()
export class EmbeddingService {
  // Храним ТОЛЬКО embeddings (без текста)
  async saveEmbedding(memoryId: string, embedding: number[], folder: string) {
    await prisma.memoryEmbedding.create({
      data: {
        memoryId,
        embedding, // Вектор
        folder,
        // НЕТ title и content!
      }
    });
  }
  
  // Поиск по embeddings
  async findSimilar(embedding: number[], limit: number = 10) {
    // Используем pgvector для поиска
    const results = await prisma.$queryRaw`
      SELECT "memoryId", folder
      FROM "MemoryEmbedding"
      ORDER BY embedding <-> ${embedding}::vector
      LIMIT ${limit}
    `;
    
    // Возвращаем только memoryId (без текста!)
    return results.map(r => ({
      memoryId: r.memoryId,
      folder: r.folder
    }));
  }
}
```

---

## 📊 Сравнение подходов

| Подход | Приватность | Производительность | Сложность | Для вашего проекта |
|--------|-------------|-------------------|-----------|-------------------|
| **Гибридный (embeddings)** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ **Лучший** |
| **Локальные AI модели** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⚠️ Сложно |
| **Шифрование** | ⭐⭐⭐⭐⭐ | ⭐ | ⭐⭐⭐⭐⭐ | ❌ Не работает |
| **Federated Learning** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ | ❌ Не подходит |

---

## 🚀 План реализации гибридного подхода

### Шаг 1: Локальное хранилище (IndexedDB)

```typescript
// frontend/lib/localDB.ts
import { openDB, DBSchema } from 'idb';

interface MemoraidDB extends DBSchema {
  memories: {
    key: string;
    value: {
      id: string;
      title: string;
      content: string;
      folder: string;
      createdAt: Date;
    };
  };
}

export const localDB = openDB<MemoraidDB>('memoraid', 1, {
  upgrade(db) {
    db.createObjectStore('memories', { keyPath: 'id' });
  }
});
```

### Шаг 2: Генерация embeddings

**Вариант A: На клиенте (TensorFlow.js)**
```typescript
import * as use from '@tensorflow-models/universal-sentence-encoder';

const model = await use.load();
const embedding = await model.embed(text);
```

**Вариант B: На сервере (без текста)**
```typescript
// Отправляем только hash для генерации embedding
const hash = await crypto.subtle.digest('SHA-256', text);
// Сервер генерирует embedding по hash (но это не работает!)
```

**Вариант C: Гибридный**
```typescript
// Генерируем embedding на сервере, но отправляем только embedding
// Сервер не хранит текст, только embedding
```

### Шаг 3: Синхронизация embeddings

```typescript
// При сохранении заметки
async function saveMemory(memory: Memory) {
  // 1. Сохраняем локально
  await localDB.memories.put(memory);
  
  // 2. Генерируем embedding (локально или на сервере)
  const embedding = await generateEmbedding(memory.title + memory.content);
  
  // 3. Отправляем на сервер ТОЛЬКО embedding
  await syncEmbedding(memory.id, embedding, memory.folder);
}
```

### Шаг 4: AI функции через локальный контекст

```typescript
async function generateAdvice(query: string) {
  // 1. Поиск релевантных заметок через embeddings
  const memoryIds = await searchByEmbedding(query);
  
  // 2. Получаем полные данные локально
  const memories = await localDB.memories.bulkGet(memoryIds);
  
  // 3. Формируем контекст локально
  const context = formatContext(memories);
  
  // 4. Отправляем на сервер ТОЛЬКО контекст
  return await fetch('/api/advice', {
    method: 'POST',
    body: JSON.stringify({ query, context })
  });
}
```

---

## ⚠️ Важные моменты

### 1. Embeddings могут раскрывать информацию

**Проблема:** Похожие тексты имеют похожие embeddings. Можно восстановить информацию.

**Решение:**
- Использовать дифференциальную приватность
- Добавлять шум к embeddings
- Ограничить доступ к embeddings

### 2. Синхронизация между устройствами

**Проблема:** Как синхронизировать данные между устройствами?

**Решение:**
- Использовать end-to-end шифрование для синхронизации
- Каждое устройство имеет свой ключ
- Синхронизация через зашифрованный канал

### 3. Производительность

**Проблема:** Генерация embeddings на клиенте может быть медленной.

**Решение:**
- Использовать Web Workers для генерации embeddings
- Кэшировать embeddings локально
- Использовать более легкие модели

---

## 📝 Итог

**Для вашего проекта лучший вариант:**

✅ **Гибридный подход:**
- Данные хранятся локально (IndexedDB)
- Embeddings на сервере (без текста)
- AI функции через локальный контекст
- Максимальная приватность + работа AI

**Это даст:**
- ✅ Данные остаются локально
- ✅ Сервер не видит содержимое
- ✅ AI функции работают
- ✅ Работает офлайн (частично)

Хотите, чтобы я помог реализовать этот подход?

