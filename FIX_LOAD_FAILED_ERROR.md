# 🔧 Исправление ошибки "Load failed"

## 🔍 Диагностика проблемы

Ошибка "Load failed" означает, что запрос не может быть выполнен из-за сетевой проблемы. Возможные причины:

1. ❌ `NEXT_PUBLIC_BACKEND_URL` не установлен или неправильный на Frontend
2. ❌ Backend сервис не работает или недоступен
3. ❌ CORS блокирует запрос
4. ❌ Неправильный URL бэкенда

---

## ✅ Шаг 1: Проверить NEXT_PUBLIC_BACKEND_URL на Frontend

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Найдите ваш **Frontend сервис** (например, `memoraid-frontend`)
3. Перейдите в раздел **"Environment"**
4. Проверьте переменную `NEXT_PUBLIC_BACKEND_URL`
5. Убедитесь, что значение правильное:
   - ✅ Должно начинаться с `https://`
   - ✅ Должно быть URL вашего Backend сервиса (например, `https://memoraid-backend.onrender.com`)
   - ✅ Не должно заканчиваться на `/`

**Пример правильного URL:**
```
https://memoraid-backend.onrender.com
```

**Если переменной нет или она неправильная:**
1. Нажмите **"Add Environment Variable"**
2. **Key**: `NEXT_PUBLIC_BACKEND_URL`
3. **Value**: URL вашего Backend сервиса (скопируйте из Backend сервиса в Render)
4. Нажмите **"Save Changes"**
5. Render автоматически перезапустит Frontend сервис

---

## ✅ Шаг 2: Проверить, что Backend сервис работает

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Найдите ваш **Backend сервис** (например, `memoraid-backend`)
3. Проверьте статус сервиса:
   - ✅ Должен быть **"Live"** (зеленый индикатор)
   - ❌ Если **"Sleeping"** - сервис спит (на Free плане это нормально, но первый запрос займет время)
   - ❌ Если **"Failed"** - сервис не работает, проверьте логи

4. Скопируйте **URL** Backend сервиса (например, `https://memoraid-backend.onrender.com`)

5. Откройте URL в браузере - должна открыться страница с информацией об API или health check

6. Попробуйте открыть health endpoint:
   ```
   https://memoraid-backend.onrender.com/health
   ```
   Должен вернуться ответ `{"status":"ok"}` или похожий

---

## ✅ Шаг 3: Проверить логи Frontend

После исправления `NEXT_PUBLIC_BACKEND_URL`:

1. Откройте Frontend сервис в Render Dashboard
2. Перейдите в раздел **"Logs"**
3. Попробуйте отправить сообщение в Mini App
4. Найдите в логах строки:
   ```
   [handleSendMessage] Backend URL: https://...
   [handleSendMessage] Request URL: https://.../ingest
   ```

**Если видите:**
- `Backend URL: http://localhost:3001` → `NEXT_PUBLIC_BACKEND_URL` не установлен (см. Шаг 1)
- `Network error: ...` → Проблема с сетью или URL неправильный

---

## ✅ Шаг 4: Проверить логи Backend

1. Откройте Backend сервис в Render Dashboard
2. Перейдите в раздел **"Logs"**
3. Попробуйте отправить сообщение в Mini App
4. Проверьте, приходят ли запросы на бэкенд

**Если запросы не приходят:**
- Проверьте, что URL правильный (см. Шаг 1)
- Проверьте, что Backend сервис работает (см. Шаг 2)
- Проверьте CORS настройки (см. Шаг 5)

---

## ✅ Шаг 5: Проверить CORS настройки

CORS должен быть настроен правильно на бэкенде. Проверьте:

1. Откройте Backend сервис → **"Environment"**
2. Проверьте переменную `FRONTEND_URL` (опционально)
3. Если её нет - это нормально, CORS настроен на разрешение всех источников

**Если CORS блокирует запросы:**
- Добавьте переменную `FRONTEND_URL` со значением URL Frontend сервиса
- Или оставьте её пустой - CORS будет разрешать все источники (для Telegram Mini App это нормально)

---

## 🔧 Решение проблем

### Проблема 1: `NEXT_PUBLIC_BACKEND_URL` не установлен

**Симптомы:**
- В логах Frontend видно `Backend URL: http://localhost:3001`
- Запросы идут на localhost вместо реального бэкенда

**Решение:**
1. См. Шаг 1 выше
2. Убедитесь, что переменная установлена и правильная
3. Подождите 1-2 минуты после сохранения, пока Frontend перезапустится

### Проблема 2: Backend сервис не отвечает

**Симптомы:**
- Backend сервис показывает статус "Failed" или "Sleeping"
- Health endpoint не отвечает

**Решение:**
1. Проверьте логи Backend - там должна быть информация об ошибке
2. Если сервис "Sleeping" - это нормально для Free плана, но первый запрос займет 30-60 секунд
3. Если сервис "Failed" - проверьте логи и исправьте ошибки

### Проблема 3: CORS ошибка

**Симптомы:**
- В консоли браузера видно ошибку CORS
- Запросы блокируются браузером

**Решение:**
1. Убедитесь, что CORS настроен правильно на бэкенде (см. Шаг 5)
2. Проверьте, что заголовки правильные в запросе
3. Убедитесь, что Backend сервис работает

### Проблема 4: Неправильный URL

**Симптомы:**
- URL начинается с `http://` вместо `https://`
- URL заканчивается на `/`
- URL содержит лишние символы

**Решение:**
1. Убедитесь, что URL правильный:
   - ✅ Начинается с `https://`
   - ✅ Не заканчивается на `/`
   - ✅ Не содержит лишних символов
2. Скопируйте URL напрямую из Render Dashboard

---

## 🧪 Тестирование после исправления

После исправления проблем:

1. Откройте Mini App в Telegram
2. Откройте консоль разработчика (если возможно)
3. Отправьте тестовое сообщение
4. Проверьте логи Frontend:
   - Должно быть: `[handleSendMessage] Backend URL: https://...`
   - Должно быть: `[handleSendMessage] Request URL: https://.../ingest`
5. Проверьте логи Backend:
   - Должен прийти запрос на `/ingest`
   - Должно быть: `[ingest] Received request: userId=...`

---

## 📝 Чеклист

- [ ] `NEXT_PUBLIC_BACKEND_URL` установлен на Frontend
- [ ] URL правильный (начинается с `https://`, не заканчивается на `/`)
- [ ] Backend сервис работает (статус "Live")
- [ ] Health endpoint отвечает (`/health`)
- [ ] В логах Frontend видно правильный Backend URL
- [ ] В логах Backend приходят запросы
- [ ] Сообщения отправляются успешно

---

## 🆘 Если проблема не решена

Если после выполнения всех шагов проблема остается:

1. **Проверьте логи Frontend** - там должна быть информация о URL и ошибке
2. **Проверьте логи Backend** - там должна быть информация о том, приходят ли запросы
3. **Проверьте, что оба сервиса работают** - Frontend и Backend должны быть "Live"
4. **Попробуйте открыть Backend URL в браузере** - должен открыться health endpoint или API info

---

## 💡 Дополнительная информация

### Что такое "Load failed"?

"Load failed" - это общая ошибка сети, которая означает, что запрос не может быть выполнен. Возможные причины:
- Неправильный URL
- Сервер не отвечает
- Сетевая проблема
- CORS блокирует запрос
- Таймаут запроса

### Почему это важно?

Без правильной настройки `NEXT_PUBLIC_BACKEND_URL` фронтенд не знает, куда отправлять запросы. Это критически важно для работы приложения.

