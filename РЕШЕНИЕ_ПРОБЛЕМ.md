# 🔧 Решение проблем

## Ошибка: "type vector does not exist"

**Проблема:** Prisma не может создать таблицы, потому что расширение pgvector не установлено в PostgreSQL.

**Решение:**

Выполните команду в терминале:

```bash
bash fix-db.sh
```

Или вручную:

```bash
# 1. Создать расширение в БД
docker exec -i infra-postgres-1 psql -U memoraid -d memoraid -c "CREATE EXTENSION IF NOT EXISTS vector;"

# 2. Применить схему
cd backend
pnpm prisma db push --accept-data-loss
```

## Ошибка: "port already in use"

**Проблема:** Порт уже занят другим процессом.

**Решение:**

```bash
# macOS/Linux - найти и убить процесс на порту
lsof -ti:3000 | xargs kill -9
lsof -ti:3001 | xargs kill -9
lsof -ti:5432 | xargs kill -9
lsof -ti:6379 | xargs kill -9
```

## Docker контейнеры не запускаются

**Решение:**

```bash
# Остановить все контейнеры
cd infra
docker compose -f docker-compose.dev.yml down

# Запустить заново
docker compose -f docker-compose.dev.yml up -d
```

## Prisma Client не сгенерирован

**Решение:**

```bash
cd backend
pnpm prisma:generate
```

## База данных не синхронизируется

**Решение:**

```bash
# Полная пересборка БД (⚠️ удалит все данные!)
cd infra
docker compose -f docker-compose.dev.yml down -v
docker compose -f docker-compose.dev.yml up -d
sleep 5
cd ../backend
pnpm prisma db push --accept-data-loss
```

## Проверка что всё работает

```bash
# Проверить Docker контейнеры
docker ps

# Проверить подключение к БД
docker exec -i infra-postgres-1 psql -U memoraid -d memoraid -c "SELECT version();"

# Проверить расширение pgvector
docker exec -i infra-postgres-1 psql -U memoraid -d memoraid -c "SELECT * FROM pg_extension WHERE extname='vector';"

# Проверить health endpoint
curl http://localhost:3001/health
```

