# 🔍 Как установить pgvector расширение

## Вариант 1: Через Render CLI (рекомендуется)

### Шаг 1: Установите Render CLI (если еще не установлен)

```bash
npm install -g @render/cli
```

### Шаг 2: Авторизуйтесь в Render

```bash
render login
```

Откроется браузер для авторизации.

### Шаг 3: Выполните команду для установки pgvector

Вы получили команду от Render:
```bash
render psql dpg-d4te71re5dus739fqnvg-a
```

Выполните её в терминале. Откроется интерактивный psql.

### Шаг 4: В psql выполните SQL команду

После открытия psql выполните:
```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

Затем выйдите:
```sql
\q
```

---

## Вариант 2: Одной командой (без интерактивного psql)

Если Render CLI установлен и вы авторизованы:

```bash
render psql dpg-d4te71re5dus739fqnvg-a -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

---

## Вариант 3: Через любой PostgreSQL клиент

Если у вас есть доступ к строке подключения PostgreSQL из Render:

1. Откройте PostgreSQL сервис в Render Dashboard
2. Перейдите в **"Info"** или **"Connections"**
3. Найдите **"Connection String"** или **"Internal Database URL"**
4. Скопируйте строку подключения (выглядит как `postgresql://user:password@host:port/dbname`)

Затем используйте любой PostgreSQL клиент:

**Через psql (если установлен локально):**
```bash
psql "postgresql://user:password@host:port/dbname" -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

**Через DBeaver, pgAdmin или другой GUI клиент:**
1. Подключитесь к БД используя строку подключения
2. Выполните SQL:
```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

---

## Вариант 4: Через Render Dashboard (если доступен Shell)

1. Откройте PostgreSQL сервис в Render Dashboard
2. Найдите раздел **"Shell"** или **"Connect"**
3. Если доступен терминал, выполните:
```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

---

## ✅ Проверка установки

После установки можно проверить:

```sql
SELECT * FROM pg_extension WHERE extname = 'vector';
```

Должна вернуться строка с информацией о расширении.

---

## 📝 Примечание

Если команда `render psql` не работает, убедитесь что:
1. Render CLI установлен: `npm install -g @render/cli`
2. Вы авторизованы: `render login`
3. Вы используете правильный ID базы данных

