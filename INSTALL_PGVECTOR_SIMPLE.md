# 🔍 Простой способ установить pgvector БЕЗ Render CLI

## ✅ Вариант 1: Через Render Dashboard (самый простой)

1. Откройте PostgreSQL сервис в Render Dashboard
2. Найдите раздел **"Connect"** или **"Info"**
3. Найдите **"Connection String"** или **"Internal Database URL"**
4. Скопируйте строку подключения (выглядит как `postgresql://user:password@host:port/dbname`)

Затем выполните в терминале:
```bash
psql "postgresql://user:password@host:port/dbname" -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

---

## ✅ Вариант 2: Через DBeaver или pgAdmin (GUI)

1. Установите [DBeaver](https://dbeaver.io/) (бесплатно) или [pgAdmin](https://www.pgadmin.org/)
2. Получите строку подключения из Render Dashboard (см. Вариант 1)
3. Подключитесь к базе данных
4. Выполните SQL:
```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

---

## ✅ Вариант 3: Пропустить на данном этапе (рекомендуется)

**pgvector можно установить позже!** Приложение будет работать без него.

Просто используйте Build Command с `prisma db push` - таблицы создадутся, приложение заработает.

---

## 📝 Рекомендация

**Просто пропустите установку pgvector сейчас.**

Используйте Build Command:
```bash
pnpm install --prod=false && pnpm exec prisma generate --schema=./prisma/schema.prisma && pnpm exec prisma db push --schema=./prisma/schema.prisma --accept-data-loss && pnpm build
```

Все будет работать! pgvector можно добавить позже через Render Dashboard или когда понадобится семантический поиск.

